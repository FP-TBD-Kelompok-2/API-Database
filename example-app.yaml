#app.yaml
runtime: python37

env_variables:
  CLOUD_SQL_USERNAME:
  CLOUD_SQL_PASSWORD:
  CLOUD_SQL_DATABASE_NAME:
  CLOUD_SQL_CONNECTION_NAME:

  CLOUD_REDIS_HOST:
  CLOUD_REDIS_PORT:
  CLOUD_REDIS_PASSWORD:

  CLOUD_MONGO_URI:
